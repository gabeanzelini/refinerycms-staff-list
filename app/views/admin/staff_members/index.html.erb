<div id='actions'>
  <ul>
    <li>
      <%= render :partial => "/shared/admin/search", :locals => {:url => admin_staff_members_url} %>
    </li>
    <li>
      <%= link_to t('.create'), new_admin_staff_member_url, :class => "add_icon" %>
    </li>
  </ul>
</div>
<div id='records'>
  <% if searching? %>
    <h2><%= t('shared.admin.search.results_for', :query => params[:search]) %></h2>
  <% end %>
  <% if @staff_members.any? %>
    <%= will_paginate @staff_members %>
    <ul>
      <%= render :partial => "staff_member", :collection => @staff_members %>
    </ul>
    <%= will_paginate @staff_members %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong><%= t('.no_items_yet') %></strong>
      <% else %>
        <%= t('shared.admin.search.no_results') %>
      <% end %>
    </p>
  <% end %>
</div>
